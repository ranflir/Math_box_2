//type (x:number, y:number) -> number;
const add =(a,b)=> a+b;

//type (x:number, y:number) -> number;
const substact = (a,b)=> a-b;

//type (x:number, y:number) -> number;
const multiple =(a,b)=> {
  return a*b;
};